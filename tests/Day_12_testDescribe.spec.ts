import { test, expect } from '@playwright/test';

test.describe.serial('Day 12 Test Describe', () => {

  test('Login', async ({ page }) => {
  // login code goes here
    
  });

  test('Select Prod and add to Cart', async ({ page }) => {
   // code to select product and add to cart goes here`
  });

   test('Cart to Checkout', async ({ page }) => {
   // code to proceed to checkout goes here
  });

    test('Logout', async ({ page }) => {
     // code to logout goes here
    });

}); 